spring:
  application:
    name: articleshelf
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/articleshelf}
    username: ${SPRING_DATASOURCE_USERNAME:articleshelf}
    password: ${SPRING_DATASOURCE_PASSWORD:articleshelf}
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
  http:
    converters:
      preferred-json-mapper: jackson2
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

server:
  port: ${PORT:8080}
  forward-headers-strategy: framework

articleshelf:
  frontend-origin: ${FRONTEND_ORIGIN:http://localhost:5173}
  auth:
    access-token-secret: ${JWT_ACCESS_SECRET:dev-articleshelf-access-secret-change-me-please-32bytes}
    access-token-ttl-seconds: ${JWT_ACCESS_TOKEN_TTL_SECONDS:900}
    refresh-token-hash-secret: ${AUTH_REFRESH_TOKEN_HASH_SECRET:dev-articleshelf-refresh-hash-secret-change-me}
    refresh-token-ttl-days: ${AUTH_REFRESH_TOKEN_TTL_DAYS:30}
    cookie-secure: ${AUTH_COOKIE_SECURE:false}
    cookie-same-site: ${AUTH_COOKIE_SAME_SITE:Lax}
    csrf-enabled: ${AUTH_CSRF_ENABLED:false}
    initial-user-enabled: ${ARTICLESHELF_INITIAL_USER_ENABLED:false}
    initial-username: ${ARTICLESHELF_INITIAL_USERNAME:owner}
    initial-user-password: ${ARTICLESHELF_INITIAL_USER_PASSWORD:password123}
  auth-rate-limit:
    enabled: ${ARTICLESHELF_AUTH_RATE_LIMIT_ENABLED:true}
    login-capacity: ${ARTICLESHELF_LOGIN_RATE_LIMIT_CAPACITY:5}
    login-window-seconds: ${ARTICLESHELF_LOGIN_RATE_LIMIT_WINDOW_SECONDS:60}
    register-capacity: ${ARTICLESHELF_REGISTER_RATE_LIMIT_CAPACITY:3}
    register-window-seconds: ${ARTICLESHELF_REGISTER_RATE_LIMIT_WINDOW_SECONDS:600}
  extension-auth:
    code-ttl-seconds: ${ARTICLESHELF_EXTENSION_AUTH_CODE_TTL_SECONDS:300}
    access-token-ttl-seconds: ${ARTICLESHELF_EXTENSION_ACCESS_TOKEN_TTL_SECONDS:86400}
    allowed-origins: ${ARTICLESHELF_EXTENSION_ALLOWED_ORIGINS:chrome-extension://ncdpeooneagfjhgnhenhakjnfflmpdbj,chrome-extension://bpkppkfmcfdpfbododebdbaaoodglnde}
    clients:
      - client-id: articleshelf-chrome-extension
        extension-id: bpkppkfmcfdpfbododebdbaaoodglnde
        redirect-uri: https://bpkppkfmcfdpfbododebdbaaoodglnde.chromiumapp.org/
      - client-id: articleshelf-chrome-extension-local
        extension-id: ncdpeooneagfjhgnhenhakjnfflmpdbj
        redirect-uri: https://ncdpeooneagfjhgnhenhakjnfflmpdbj.chromiumapp.org/
  ogp:
    proxy-url: ${ARTICLESHELF_OGP_PROXY_URL:}
    require-proxy-in-prod: ${ARTICLESHELF_OGP_REQUIRE_PROXY_IN_PROD:false}
