# 2026-05 Backlog Archive

2026年5月に完了した Backlog タスクの要約を記録します。

## 完了タスク

- SECURITY.md 追加: repository root に security policy を追加し、public issue を避ける private reporting flow、supported scope、報告時に含めてほしい情報、best-effort の対応プロセスを明記した。README と security specs から入口を辿れるようにした。
- Flyway baseline migration の意図整理: V1 baseline migration が新規 schema と既存開発 DB の統合救済を兼ねる理由を data architecture / data specs に明記し、適用済み migration は rewrite せず新しい versioned migration で進める方針と PostgreSQL 実体での確認方針を testing docs に反映した。
- Backend quality gates の見せ方整理: README、backend architecture、CI / CD architecture、testing docs に、SpotBugs、Clean Architecture dependency test、domain / application coverage threshold、PostgreSQL IT、E2E の役割分担と CI 上の段階構成を追記した。
- Frontend architecture highlights 整理: frontend architecture に feature-oriented 構成、shared boundary、auth-aware API client、client-side domain helpers、safe Markdown rendering、responsive UX の見どころを追加し、README、security specs、testing docs の説明を現行実装に合わせた。
- Frontend app providers 分離: `main.ts` から Pinia、i18n、Vuetify theme / defaults / locale messages の設定を `frontend/src/app/providers/` へ分離し、frontend architecture docs に app-level provider 境界を追記した。
- 詳細編集でステータス変更時に既読日を同期する: 一覧カードのステータス切り替えと同じ既読日同期ルールを domain helper 化し、詳細編集で既読化時は未設定なら操作日、未読化時は null、既存既読日は維持するようにした。feature / UI / design docs と unit test も更新した。
- TagManagement DOM 計測責務分離: タグ管理の sort select 幅計測を `useMeasuredSelectWidth` に分離し、`useTagManagementState` をタグ検索、並び替え、ダイアログ state に集中させた。frontend architecture docs と unit test も更新した。
- Backend Docker image non-root 実行: backend production Docker final image に dedicated `articleshelf` user / group を追加し、jar の owner と runtime user を non-root にした。security / deployment docs と Backlog archive を同期した。
- TimeProvider / IdGenerator port 導入: 認証ユースケースに `Clock`、`IdGenerator`、`SecureRandom` を注入し、register、refresh token family、token invalidation、CSRF token 発行の実行環境依存値を固定値でテストできるようにした。backend architecture docs と auth unit test を更新した。
- Thumbnail cache eviction 導入: IndexedDB thumbnail cache に最大 200 records / 50MB の LRU eviction を追加し、期限切れ failure cache も削除対象にした。CORS 非対応画像は proxy せずプレースホルダー表示へ倒す方針を architecture / design docs に反映し、eviction unit test を追加した。
- OGP HTML 解析と charset 対応改善: OGP HTML 抽出を `OgpHtmlParser` に分離し、quoted / unquoted 属性、HTML entity、relative image URL、`Content-Type` charset、meta charset、UTF-8 fallback を扱うようにした。security / api-flow docs と parser unit test を更新した。
- 記事詳細・追加フォーム component 分割: 詳細閲覧セクションを `ArticleDetailViewSections`、メモ編集 / preview を `ArticleNotesEditor`、追加モーダルの create form state / validation / submit 変換を `useArticleCreateForm` に分離した。architecture / design docs と create form unit test を更新した。
- Frontend 検索・フィルタ仕様の契約整理: Frontend search に概要を追加して Backend search contract と合わせ、タグ複数選択を OR 条件として specs / UI docs に明記した。タイトル sort / search lowercase 比較は現在 UI locale に追従するようにした。
- Frontend API client 強化: `AbortSignal` forwarding、production `VITE_API_BASE_URL` validation、test reset helper を追加し、既存の refresh retry、CSRF header、Accept-Language、error mapping を維持する unit test と docs を更新した。
- Frontend focus management と reduced motion 改善: 追加モーダル close 後の opener focus 復帰、カレンダー日付セルの keyboard open / close と focus 復帰、`prefers-reduced-motion` の global CSS を追加し、responsive / testing docs を更新した。
- Frontend CSP と Markdown security tests 強化: Cloudflare Pages `_headers` に CSP / nosniff / Referrer-Policy / HSTS / Permissions-Policy を追加し、Markdown sanitization tests に `data:` image、SVG、iframe、style attribute、malformed HTML、external link rel を追加した。security / deployment / testing docs も更新した。
- Frontend quality gates 強化: ESLint、Vitest coverage threshold、bundle size check、Playwright axe accessibility scan を導入し、GitHub Actions の frontend check / E2E と README / CI / testing docs を更新した。
- Production DB TLS 起動ガード追加: production profile 起動時に `SPRING_DATASOURCE_URL` の `sslmode=require` / `verify-ca` / `verify-full` を検証し、TLS なしの PostgreSQL 接続を拒否するようにした。security / deployment docs と validator test も更新した。
- Supply chain security scans 追加: Dependency Review、CodeQL、Trivy filesystem scan、backend Docker image scan の workflow を追加し、Dependabot との役割分担と action pinning 方針を CI / security / testing docs に反映した。
- OGP 取得の transaction 外分離: `ArticleService` に `TransactionOperations` を導入し、URL 重複確認と保存 transaction の外で OGP fetch を同期実行するようにした。api-flow / security docs と application unit test も更新した。
- Machine-readable API エラー導入: API error response に `code` と validation 用 `fieldErrors` を追加し、既存 `messages` と `existingArticleId` を維持した。frontend API client、api errors docs、handler unit test も更新した。
- Frontend 認証 session contract 整理: JWT `exp` decode を `shared/auth/jwt`、期限前 refresh timer を dedicated service に分離し、登録時の表示名を backend と同じ任意入力へ合わせた。auth specs / frontend architecture / auth design docs と unit test も更新した。
- Backend observability metrics 追加: request ID filter、Micrometer metrics port / adapter、OGP fetch timer、auth failure / rate limit counters、article created / updated counters を追加し、quality / testing docs に個人情報を tag に含めない方針を反映した。
- セッション Cookie と CSRF 責務の分離: refresh / CSRF cookie 発行・削除を `SessionCookieWriter`、CSRF 照合を `CsrfTokenValidator` に分離し、AuthController / UserController の cookie 操作重複を削除した。auth token docs / backend architecture docs と validator unit test も更新した。
- PDF インポート実データ品質改善: PDF 由来の抽出失敗パターン、補正フロー、エラー表示方針、代表確認ケースを functional requirements と testing docs に整理した。
- カレンダー domain helper と keyboard 操作改善: 日付 key / 月 key / 6週セル生成を `features/articles/domain/calendar.ts` に分離し、unit test を追加した。既存の Enter / Space 操作と focus 復帰を calendar specs / design docs に反映した。
- OGP SSRF DNS rebinding / TOCTOU 対策強化: OGP 接続直前に DNS を再解決し、連続解決結果の変化や危険 IP 混入を拒否する `validateForConnection` を追加した。redirect 先にも同じ検証を適用し、security specs と guard unit test を更新した。
- Refresh token rotation atomic 化: refresh token lookup に pessimistic write lock を付け、旧 token の `revokedAt is null` 条件付き update が成功した場合だけ replacement token を有効化するようにした。失敗時は replacement を即時失効し、auth / security / testing docs と unit test を更新した。
- Web adapter 境界の責務整理: `ArticleRequest` を controller から独立させ、`ArticleRequestMapper` と `ClientRequestContext` を追加して request DTO 変換と client context 取得を adapter helper へ分離した。API / backend architecture docs も更新した。
