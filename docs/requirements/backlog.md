# ArticleShelf Backlog

最終更新: 2026-05-09

このドキュメントは、ArticleShelf の今後やるべきこと、残作業、構想段階の案、技術的補強候補を集約する唯一の一覧です。
各仕様書や設計書には現在の仕様・設計・運用だけを書き、将来対応や検討事項はこのファイルへ集約します。

## 1. 優先度高

### 1.1 運用・監視

- 公開環境 smoke test / 監視方針整理

## 2. 優先度中

### 2.1 UI / UX

- お気に入り導線の強化
- お気に入り / ピン留めの視覚表現
- タグのバッジ化による視認性向上
- 夜間モード / ダークテーマ対応
- レスポンシブ表示の実機磨き込み
  - 実機での `375x667` / `390x844` / `430x932` / `820x1180` / `1366x768` / `1440x900` 表示の継続確認
  - 長い日本語 / English 文言、タグ数が多いケース、Markdown 表示の追加確認

### 2.2 既存機能の補強

- OGP サムネイルの手動再取得
- IndexedDB 外への画像ファイル保存
- 検索 / フィルタ / 並び替えの性能検証

### 2.3 アカウント運用

- 退会後データの物理削除ポリシー整備
- データエクスポート
- `displayName` 変更 API / UI

### 2.4 デプロイ / セキュリティ運用

- custom domain 設定
- Supabase 接続の `sslmode=verify-full` と CA certificate 導入
- 複数インスタンス構成へ移行する場合の Redis / proxy / WAF 側 rate limit

## 3. 優先度低

### 3.1 テスト基盤

- OGP stub server の採用
- Testcontainers PostgreSQL の採用
- mobile / responsive E2E の対象ケース拡充

### 3.2 将来構想

- Chrome 拡張機能からのクイック登録
  - 閲覧中ページを未読として即登録
  - 閲覧中ページを既読として即登録
- 画像やスクリーンショットの保存と参照
- OCR による記事情報抽出
- AI 要約 / ハイライト / メタ情報抽出
- クラウド同期
- access token 即時失効の秒単位精度が不足する場合の token version 方式
