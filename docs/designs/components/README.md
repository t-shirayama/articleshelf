# Component Design

ArticleShelf の画面別コンポーネント設計をまとめる。
レスポンシブ全体のサイズ別方針は [../responsive/README.md](../responsive/README.md)、スマホ固有の詳細は [../responsive/mobile.md](../responsive/mobile.md) に従う。

## 1. 共通 UI ルール

UIの基本部品には Vuetify を使用します。
ボタン、入力欄、カード、ダイアログ、チップ、日付入力などは Vuetify を優先し、ArticleShelf固有の余白・背景・レイアウトはカスタムCSSで調整します。
保存、追加、編集、閉じる、キャンセル、削除などの主要アクションは、押せる領域が明確に見える共通ボタンスタイルを使います。
非破壊のモーダルを閉じる操作は「閉じる」、削除確認など進行中の確認を取りやめる操作は「キャンセル」と表記します。
Vuetify のUIロケールは現在の表示言語に追従し、カレンダーや入力補助UIも日本語 / English で表示します。
日付を画面に表示する場合は現在の表示言語に応じて `ja-JP` / `en-US` の表記にし、APIや永続化で扱う日付値は既存仕様に合わせて `YYYY-MM-DD` を維持します。
日付ピッカーは現在のロケールに追従し、日曜は赤系、土曜は青系で見分けられるようにします。

## 2. シェル / デスクトップレイアウト

- 左サイドバー: ステータス / お気に入り / カレンダーのナビゲーション
- アプリのファビコンは左サイドバー左上のブランドマークに合わせる
- 左サイドバーのブランドコピーは日本語 / English ともに1行で表示し、アプリ名の下で改行による高さ揺れを起こさない
- デスクトップの左サイドバー幅は英語のブランドコピー、長いナビゲーション名、言語切替、ログアウトボタンが見切れない幅を確保する
- 左サイドバーの選択表示は現在表示中の画面を優先し、カレンダー画面では未読 / 既読 / お気に入りなど一覧フィルタの選択色を出さない
- 左サイドバーと中央メインの上下 padding は揃え、画面内の開始位置を整列させる
- 左サイドバー下部: 画像付きの学習継続メッセージ
- 学習継続メッセージは約100件のコピーと30種類程度のSVGイラストモチーフを持ち、画面遷移・記事追加・サイドメニュー選択時に切り替える
- アカウント設定とログアウトは左サイドバー下部に枠付きボタンとして表示し、通常ナビゲーションやユーザー名と区別できるようにする
- 左サイドバー下部にはユーザー名、その下に日本語 / English の言語切替トグル、アカウント設定、ログアウトボタンの順で表示する。言語切替はアカウント単位ではなく端末側の表示設定として扱う
- 中央メイン: 記事一覧を表示
- 一覧画面のタイトルは、左サイドバーで選択中の項目や上部フィルタ条件に合わせて表示する
- カレンダー画面では、登録日と既読日の表示モードを切り替え、月内の追加数・既読数・積読差分を表示する
- 記事を選択したら、一覧と同時表示せず詳細画面へ遷移
- 詳細画面では、本文領域とメタ情報 / 操作ボタンを分けて表示
- 記事が増えても画面全体ではなく、記事一覧領域の中でスクロールする
- 記事一覧やカレンダー内のスクロールバーは、カードより目立たない薄いトーンで表示する
- 記事一覧領域はスクロールバーの有無で記事カード幅が変わらないよう、スクロールバー用の余白を安定して確保する

## 3. モーダル共通ルール

- 新規作成・編集・絞り込みなど操作完了を伴うモーダルは、タイトルと主要アクションを同じヘッダー行に配置する
- ヘッダー行の左側にタイトル、右側に「閉じる」と保存・適用などの主要アクションを並べ、本文末尾に同じアクションを重複配置しない
- 条件クリアやリセットなど補助アクションがある場合も、文脈上ヘッダー行に収まるものはタイトル行にまとめる
- フォーム本文とヘッダー行の左右余白を揃え、入力ブロックは薄い横線と一定の縦余白で反復する
- モバイルではヘッダーを縦積みにし、アクションボタンを横幅いっぱいにして押しやすさを優先する

## 4. 詳細コンポーネント

- [記事関連コンポーネント](articles.md): ArticleCard、ArticleDetail、ArticleFormModal、SearchFilterBar
- [カレンダー / タグ管理](calendar-and-tags.md): CalendarView、TagManagementView
- [認証画面](auth.md): AuthScreen
