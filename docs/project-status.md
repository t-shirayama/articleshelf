# ReadStack Project Status

最終更新: 2026-05-06

## 1. 現在の状況

ReadStack は、記事の登録・一覧表示・詳細編集・削除までを一通り触れる MVP の土台ができています。  
フロントエンドは Vue 3 + Pinia + Vuetify で一覧画面と詳細画面を切り替える操作フローが実装されており、バックエンドは Spring Boot + PostgreSQL で REST API と永続化が動く構成です。
`docker-compose.yml` には `frontend` / `backend` / `db` が定義されており、ローカルでまとめて起動できます。フロントエンドとバックエンドは開発時のホットリロードにも対応しています。

## 2. 実装済み

### 2.1 フロントエンド

- 記事一覧表示
- タイトル / URL / 概要 or メモ / タグ / 読了日 / ステータスの表示
- 検索キーワードによる絞り込み
- 未読 / 読了 / お気に入りの切り替え
- サイドバーからのタグ絞り込み
- 記事追加モーダル
- 記事選択後の詳細画面での編集
- 記事削除
- レスポンシブを意識した一覧 / 詳細の画面切り替え
- Vuetify によるボタン、入力、カード、ダイアログ、チップ、日付入力の共通化

### 2.2 バックエンド

- `GET /api/articles`
- `GET /api/articles/{id}`
- `POST /api/articles`
- `PUT /api/articles/{id}`
- `DELETE /api/articles/{id}`
- `GET /api/tags`
- `POST /api/tags`
- URL 重複チェック
- OGP 取得によるタイトル / 概要の補完
- PostgreSQL への永続化
- CORS 設定
- バリデーションエラー / ドメイン例外の API 応答

### 2.3 開発環境

- Docker Compose による `frontend` / `backend` / `db` の一括起動
- Vite によるフロントエンドのホットリロード
- Spring Boot DevTools と Maven compile 監視によるバックエンドの自動再起動

### 2.4 データモデル

- `Article`
  - `url`
  - `title`
  - `summary`
  - `status`
  - `readDate`
  - `favorite`
  - `notes`
  - `createdAt`
  - `updatedAt`
- `Tag`
- 記事とタグの関連付け

## 3. 仕様との差分

現状の実装は要件の中核を概ね満たしていますが、ドキュメント上の理想と比べると次の差分があります。

- `favorite` は実装済みだが、仕様書では説明が薄く、画面仕様との対応が明確ではない
- OGP はタイトルと概要の補完までで、サムネイル保存・表示は未対応
- タグ管理 API はあるが、独立したタグ管理画面はまだない
- フロントエンドは `Vue Router` 前提の記述がある一方、実装は Pinia とコンポーネント状態による一覧 / 詳細の画面切り替え構成
- 検索 / フィルタはクライアントから使えるが、並び替えや複数タグ選択は未対応
- 画像添付、スクリーンショット起点の入力、OCR/AI 系機能は未着手

## 4. 未実装・残作業

### 4.1 MVP 仕上げで優先度が高いもの

- エラーハンドリング改善
  - OGP 取得失敗時の画面表示
  - API 失敗時の再試行や入力保持
- 入力体験の改善
  - バリデーションメッセージの見える化
  - タグ入力 UI の改善
  - 保存中状態の表示
- 一覧画面のモチベーション導線追加
  - `docs/designs/desktop_article_list.png` の左下イメージに近い位置へ、画像付きで「今日も1つ学びをストックしよう」のような継続を後押しするメッセージを表示する
  - 単なる案内ではなく、記事をストックする意欲が上がるコピー・見た目にする
- 記事追加モーダルの改善
  - タグは自由入力に加えて、過去に追加したタグから選択できるようにする
  - ステータス選択は廃止し、「あとで読む」のチェックボックスに置き換える
  - 「あとで読む」はデフォルトでチェック済みにし、チェック済みなら未読として保存する
- 記事一覧カードの操作改善
  - トップページの記事一覧でも、各記事をお気に入りに追加 / 解除できるようにする
- スクロール設計の改善
  - 記事一覧が増えた場合は記事一覧エリア内でスクロールする
  - タグが増えた場合はタグエリア内でスクロールする
  - 画面全体にスクロールが発生しないレイアウトを目指す
- 仕様ドキュメントの更新
  - 実装済み機能に合わせて仕様書・設計書を同期
- テスト追加
  - 一通りの実装が揃った後に、バックエンドのサービス / コントローラーの単体・結合テストを整備
  - 一通りの実装が揃った後に、フロントエンドのストア / コンポーネントのテストを整備

### 4.2 ドキュメント整理タスク

- `docs/specification.md` の分割
  - 今後肥大化しないように、`docs/specification/` フォルダを作成して機能ごとに仕様ファイルを分割する
  - 例: 記事一覧、記事詳細、記事追加、タグ管理、検索・フィルター、API、データモデル
  - ルートの `docs/specification.md` は全体概要と各ファイルへの目次として残す
- `docs/requirements.md` の分割
  - 今後肥大化しないように、機能要件と非機能要件でフォルダを分け、項目ごとに要件ファイルを分割する
  - 例: `docs/requirements/functional/` と `docs/requirements/non-functional/`
  - ルートの `docs/requirements.md` は全体概要と各ファイルへの目次として残す

### 4.3 次の機能追加候補

- 複数タグ選択フィルタ
- 並び替え
  - 追加日順
  - 読了日順
  - 更新日順
- タグ管理画面
- OGP サムネイルの保存・表示
- お気に入り導線の強化
- モバイル操作の磨き込み

### 4.4 将来拡張

- 画像 / スクリーンショット添付
- OCR による記事情報抽出
- AI 要約 / ハイライト
- ユーザー認証
- マルチユーザー対応
- クラウド同期

## 5. 技術的な注意点

- バックエンドは `spring.jpa.hibernate.ddl-auto=update` のため、開発中は便利ですが、本番運用前にはマイグレーション方式へ寄せたいです
- `frontend/package.json` にはテスト・lint 用スクリプトがまだありません
- 検索やタグ絞り込みは API 呼び出しで実現できていますが、データ量が増えたときの性能検証はこれからです
- README には「これから実装する前提」の表現が残っているため、現状に合わせた更新余地があります

## 6. おすすめの次アクション

1. 記事追加モーダルのタグ選択 / 入力と「あとで読む」チェックボックス化を進める
2. 一覧画面のモチベーション導線・お気に入り操作・エリア内スクロールを整える
3. 仕様書 / 要件定義を分割して、次の機能追加でドキュメントが肥大化しない土台を作る
4. 仕様書 / 設計書を実装に合わせて更新し、次の機能追加に備える
5. 一通りの実装が揃った後にテスト基盤を整備する
